# RA-GARK Handoff Document

This folder contains the LaTeX source for the project handoff document.

Compile from this directory:

```bash
latexmk -xelatex -interaction=nonstopmode main.tex
```

The source is intentionally kept under `Code/handoff/` so it travels with the
code handoff material rather than the thesis source.
