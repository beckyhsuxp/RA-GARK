# Local Agent Notes

This file supplements the repository-level instructions from the parent `AGENTS.md`.

## Workflow Rule

- Whenever `notes/SLIDES.md` changes, update `notes/script.md` in the same turn.
- After updating either file, commit and push the change together.
